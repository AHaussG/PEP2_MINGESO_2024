server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/AHaussG/PEP2_MINGESO_2024
          search-paths: config-data
  application:
    name: config-service
